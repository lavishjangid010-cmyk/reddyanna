// This folder is for backend logic, controllers, and database connections.
// You can add your routes and controllers here.

export const backendConfig = {
  version: "1.0.0",
  environment: process.env.NODE_ENV || "development"
};
